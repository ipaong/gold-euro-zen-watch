# XAUEUR Signal Lab — แผนงานรอบแรก (Phase 1)

แอปช่วยตัดสินใจเทรด XAUEUR (ทองคำสกุลยูโร) บน timeframe M15 พยากรณ์ 5 แท่งถัดไป (~75 นาที)
ด้วย "นักวิเคราะห์ 5 คนที่โหวต + หัวหน้าทีมอีก 1 คนที่สรุป" แล้วออกมาเป็น ซื้อ / ขาย / รอ
ไม่มีการส่งคำสั่งเทรดอัตโนมัติ

รอบนี้ทำ Phase 1 เท่านั้น: หน้าจอครบทุกหน้า + เครื่องยนต์พยากรณ์ที่คำนวณจริง + ข้อมูลเดโมสมจริง
เก็บข้อมูลในเบราว์เซอร์ (ยังไม่ต่อฐานข้อมูล) โครงสร้างโค้ดออกแบบให้สลับไปใช้ฐานข้อมูลจริง/ข้อมูลจริงได้ทีหลัง

ภาษาในแอป: ไทยทั้งหมด

## หน้าจอที่จะได้

1. **หน้าแรก (แดชบอร์ด)** — เรียงตามความสำคัญ
   - ราคา XAUEUR ปัจจุบัน, % เปลี่ยนแปลง, เวลาอัปเดตล่าสุด, สถานะตลาด
   - การ์ดใหญ่สุด: **สัญญาณรวม** 🟢 ซื้อ / 🔴 ขาย / ⚪ รอ + "โมเดลเห็นด้วย 4/5" + ความมั่นใจ % + ระดับความเสี่ยงข่าว
   - กราฟแท่งเทียน: แท่งจริงสีทึบ, 5 แท่งพยากรณ์โปร่งแสง + เส้นแบ่ง "จากนี้คือการพยากรณ์"
   - การ์ดโมเดลที่โหวต 5 ใบ + การ์ด Ensemble แยกอีก 1 ใบ (ตัวสรุป ไม่มีสิทธิ์โหวต) แตะเพื่อดูรายละเอียด
   - "เกิดอะไรขึ้น?" / "ทำไมถึงซื้อ?" / "อะไรจะทำให้ผิดพลาด?" เป็นภาษาคนธรรมดา
   - 5 ฉากทัศน์อนาคต พร้อมลูกศร ↑↓ และ "น้ำหนักฉากทัศน์ %"
   - การ์ดวางแผนเทรด: แนวรับ/แนวต้านใกล้ ๆ, โซนที่ถือว่าคิดผิด, ATR, ระดับความเสี่ยง
   - ปฏิทินข่าวสำคัญที่กำลังจะมา + คำเตือน "ข่าวแรงอีก 18 นาที"
   - สรุปความแม่นล่าสุดแบบย่อ

2. **หน้าย้อนเวลา (Time Machine)** — เลือกวัน–เวลาในอดีต ระบบจะเห็นข้อมูลแค่ถึงเวลานั้น
   กด "ล็อกการพยากรณ์" → กด "เปิดเผยอนาคต" → เทียบพยากรณ์กับของจริงและให้คะแนน

3. **หน้าสมุดบันทึกการพยากรณ์** — รายการพยากรณ์ทั้งหมด เปิดดูย้อนหลังได้ตรงตามที่เห็นตอนนั้น
   (แอปไม่มีทางแก้ไขพยากรณ์ที่ล็อกแล้ว และระบบไม่เขียนทับย้อนหลัง)

4. **หน้าคะแนนโมเดล** — ความแม่นของแต่ละโมเดลและของสัญญาณรวม, เลือกช่วง 20 / 50 / 100 / ทั้งหมด,
   แยกความแม่นฝั่งซื้อ–ขาย, ความถี่ที่บอก "รอ", ความมั่นใจเทียบกับความแม่นจริง

5. **หน้าข่าว & มหภาค** — ทิศทางทอง, ทิศทางยูโร, ผลรวมต่อ XAUEUR, ปฏิทินเศรษฐกิจ (🔴🟡⚪)

6. **หน้าตั้งค่า** — เกณฑ์ความมั่นใจ, จำนวนโมเดลขั้นต่ำที่ต้องเห็นด้วย, ช่วงเวลาเลี่ยงข่าวแรง
   (คู่เงินและ timeframe ล็อกไว้ที่ XAUEUR / M15)

7. **หน้าจอต้อนรับครั้งแรก** — อธิบายสั้น ๆ ว่าแอปทำอะไร ไม่ทำอะไร แล้วกด "เริ่มโหมดเดโม"

## กฎสำคัญที่จะเขียนลงในระบบ

- **5 เสียงที่โหวต คือ** Trend, Momentum, Technical/Price Structure, News & Macro,
  Volatility/Statistical — **Ensemble ไม่มีสิทธิ์โหวต** ทำหน้าที่สรุปผลจาก 5 โมเดลอีกชั้นหนึ่ง
  พร้อมคะแนนความมั่นใจของตัวเอง เพื่อไม่ให้นับข้อมูลซ้ำ
- **ไม่บังคับให้เกิดสัญญาณ** — 3/5 เสียงไม่ได้แปลว่าซื้อทันที ต้องผ่านด่านคุณภาพ:
  จำนวนเสียงเห็นด้วย ≥ เกณฑ์, ความมั่นใจเฉลี่ยเหนือเกณฑ์, ข้อมูลสดพอ, ไม่มีแหล่งข้อมูลล่ม,
  ไม่มีข่าวแรงใกล้เกินไป, โมเดลไม่ขัดแย้งรุนแรง — ไม่ผ่านข้อไหนก็ตอบ **รอ** พร้อมบอกเหตุผลว่าติดข้อใด
- **ไม่ปั้นข่าว** — ยังไม่ต่อข่าวจริง จึงติดป้าย "ข่าวเดโม" ชัดเจนทุกจุด และทั้งแอปติดป้าย "ข้อมูลเดโม"
- **ทิศทางและราคาพยากรณ์ต้องคำนวณจากสภาพตลาด ณ เวลานั้น** ไม่ใช่การสุ่ม:
  ใช้เทรนด์, โมเมนตัม, ATR, ความผันผวน, แนวรับ–แนวต้าน และ market regime เป็นตัวกำหนด
  ส่วนการสุ่มแบบมีเมล็ดคงที่ (seeded) ใช้ได้เฉพาะทำให้ 5 ฉากทัศน์ต่างกันเล็กน้อยเท่านั้น
  input เดิม → output เดิมเสมอ ไม่ใช้ AI เดาราคา
- **แยกข้อมูลตลาดเดโมออกจากเครื่องยนต์พยากรณ์เด็ดขาด** — สร้างชุดข้อมูลย้อนหลังไว้ล่วงหน้าแล้ว
  freeze เก็บเป็นไฟล์ข้อมูล เครื่องยนต์พยากรณ์อ่านมันเหมือนข้อมูลจากภายนอก
  ห้ามใช้ตรรกะเดียวกันทั้งสร้างตลาดและสร้างคำพยากรณ์ ไม่อย่างนั้น Time Machine จะดูแม่นเกินความจริง
- **พยากรณ์ที่ล็อกแล้ว** — แก้ไม่ได้ผ่านตัวแอป และระบบไม่เขียนทับย้อนหลัง
  (Phase 1 เก็บใน localStorage จึงยังไม่เรียกว่า immutable ระดับฐานข้อมูล — ความคงทนจริงและ audit trail
  ไว้ทำ Phase 2)
- ไม่ใช้คำว่า "การันตี" หรือ "ชัวร์" ที่ไหนเลย
- **เป้าหมายของ Phase 1 คือทดสอบ workflow และ UX** ไม่ใช่พิสูจน์ว่าทำนาย XAUEUR แม่นแค่ไหน
  เพราะยังเป็นข้อมูลเดโม — จะเขียนประโยคนี้ไว้ในแอปด้วย

## หน้าตา (ธีม Warm Paper)

พื้นครีมกระดาษ `#F7F4EE`, ตัวหนังสือน้ำตาลเข้ม `#2B2721`, เขียวสงบ `#3F7D62` = ฝั่งซื้อ,
แดงอิฐนุ่ม `#B0524B` = ฝั่งขาย, เหลืองอำพันสำหรับความเสี่ยง/ข่าว
มีโหมดสว่างและโหมดมืด, การ์ดมุมโค้ง, เว้นที่ว่างเยอะ, เงาบางเบา, ไม่มีสีวูบวาบ
ออกแบบมือถือก่อน มีแถบเมนูล่าง 5 ปุ่ม แล้วขยายเป็นหลายคอลัมน์บนจอใหญ่

## รายละเอียดทางเทคนิค

- TanStack Start + React + TypeScript, Tailwind v4, โทเคนสีทั้งหมดใน `src/styles.css`
- แยกชั้นชัดเจน:
  - `src/data/` — **ชุดข้อมูลตลาดเดโมที่ freeze แล้ว**: สร้างครั้งเดียวด้วยสคริปต์แยก
    (ขาขึ้น/ขาลง/ออกข้าง/เบรกเอาต์/ผันผวนสูง) เก็บเป็นไฟล์ข้อมูลนิ่ง ๆ ไม่มีตรรกะพยากรณ์ปนอยู่
  - `src/lib/market/` — `MarketDataProvider` (อินเทอร์เฟซ) + `FrozenDatasetProvider` ที่แค่อ่าน `src/data/`
    มี `getCandlesUpTo(timestamp)` ตัดข้อมูลหลังเวลาที่เลือกออกทั้งหมด กันข้อมูลรั่วจากอนาคต
  - `src/lib/indicators/` — EMA 20/50/200, RSI, MACD, ATR, swing high/low, แนวรับแนวต้าน, market regime
  - `src/lib/models/` — 5 โมเดลที่โหวต: trend, momentum, technical, news, volatility
    แต่ละตัวคืน `{ direction, confidence, summary, factors, risks }`
  - `src/lib/ensemble/` — ตัวสรุปชั้นบน อ่านผลของ 5 โมเดล คืนทิศทาง + ความมั่นใจ + เหตุผล (ไม่โหวต)
  - `src/lib/forecast/` — เครื่องยนต์สร้างแท่งพยากรณ์และ 5 ฉากทัศน์ จากตัวเลขตลาดจริง
    (drift จากเทรนด์/โมเมนตัม, ขนาดแท่งจาก ATR, เพดานจากแนวรับแนวต้าน) + seeded variation ต่อฉากทัศน์
    แยกไฟล์คนละส่วนกับ `src/data/` โดยเด็ดขาด
  - `src/lib/consensus/` — ด่านคุณภาพสัญญาณ
  - `src/lib/news/` — `NewsProvider` + `MockNewsProvider` (ปฏิทินเศรษฐกิจ EUR/USD ติดป้ายเดโม)
  - `src/lib/analysis/` — อินเทอร์เฟซ `AnalysisProvider` เตรียมไว้ต่อ AI ทีหลัง (รอบนี้ใช้ตัวสรุปแบบเทมเพลตภาษาไทย)
  - `src/lib/scoring/` — ให้คะแนน: ความแม่นทิศทาง, MAE, ค่าคลาดเคลื่อนราคาปิด/สูง/ต่ำ, แม่นรายแท่ง
  - `src/lib/store/` — เก็บพยากรณ์/ผลคะแนน/ตั้งค่าใน localStorage ผ่านอินเทอร์เฟซเดียว
    (Phase 2 เปลี่ยนเป็นฐานข้อมูลได้โดยไม่แตะ UI)
- กราฟแท่งเทียนเป็นคอมโพเนนต์ SVG เล็ก ๆ ของเราเอง เน้นอ่านง่ายและแยกแท่งพยากรณ์ให้ชัด
  ไม่ทำ zoom/pan/วาดเส้น/อินดิเคเตอร์ซ้อน ไม่เลียนแบบ TradingView
- เส้นทาง: `/`, `/time-machine`, `/journal`, `/journal/$id`, `/performance`, `/news`, `/settings`
- ทุกไฟล์เล็ก แยกคอมโพเนนต์ ใช้ shadcn ที่มีอยู่แล้ว
- ทุกปุ่มที่เห็นต้องใช้งานได้ ถ้ายังไม่พร้อมให้ขึ้น "เร็ว ๆ นี้"
- เพาะข้อมูลพยากรณ์ย้อนหลังไว้พอให้หน้าคะแนนดูมีความหมาย ติดป้าย "เดโม" ทุกแถว

## ยังไม่ทำในรอบนี้

ฐานข้อมูล Lovable Cloud, ข้อมูลตลาดจริง, ข่าวจริง, AI จริง, สะพาน MetaTrader 5 (จะเขียนเป็นเอกสาร
โครงสร้างไว้ให้), แจ้งเตือน LINE/Telegram/อีเมล/push — ทั้งหมดเป็น Phase 2–4
